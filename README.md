# jerit-pos-deploy

Built files for the POSFIX sites, and the workflow that deploys them to
Firebase Hosting. Nothing here is source code: every file is what the live
sites already serve to their visitors.

The source lives in a private repository, where it is built and tested
before anything is committed here.
